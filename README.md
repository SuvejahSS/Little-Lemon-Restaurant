# Little Lemon Capstone - Improved Submission

This React version is structured to address the capstone rubric: semantic HTML, responsive desktop/mobile UI, meta and Open Graph tags, a child BookingForm component, parent-managed available times, client-side validation, and unit tests.

## Run
npm install
npm start

## Test
npm test

## Build
npm run build
